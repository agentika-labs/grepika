{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Grep",
        "hooks": [
          {
            "type": "command",
            "command": "echo '⚠️  Consider using mcp__agentika-grep__search instead of built-in Grep for better ranked results'"
          }
        ]
      },
      {
        "matcher": "Glob",
        "hooks": [
          {
            "type": "command",
            "command": "echo '⚠️  Consider using mcp__agentika-grep__toc (directory tree) or mcp__agentika-grep__relevant (find files) instead'"
          }
        ]
      }
    ]
  }
}
