# macOS metadata
**/.DS_Store

# Rust build artifacts
/target/

# Project-specific index database (users generate their own)
.agentika-grep/

# Claude Code local settings (but track plugins/skills)
.claude/settings.local.json
